import React from "react";
import styled from "styled-components";

const ListTodo = styled.div`
  width: 100%;
  height: auto;
  background: none;
  font-size: 25px;
`;

function List() {
  return <ListTodo />;
}

export default List;
